<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/folder_default_color"/>  <!-- Default background color (yellow) -->
    <corners android:radius="16dp"/>  <!-- Set the border radius (rounded corners) -->
    <stroke
        android:color="#000000"
    android:width="2dp"/>
</shape>